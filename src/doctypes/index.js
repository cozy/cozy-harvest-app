export const KONNECTORS_DOCTYPE = 'io.cozy.konnectors'
export const TRIGGERS_DOCTYPE = 'io.cozy.triggers'

// the documents schema, necessary for CozyClient
export default {
  triggers: {
    doctype: TRIGGERS_DOCTYPE
  }
}
